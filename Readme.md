#Commands to Run this program in dev enviornment

##Gulp commands
######Gulp build -- This will start the project, open the browser to localhost, start scss compiler to css, reload browser on changes.

